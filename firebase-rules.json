{
  "rules": {
    "lists": {
      "$listId": {
        ".read": "auth != null",
        ".write": "auth != null",
        "sharedUsers": {
          ".read": "auth != null",
          ".write": "auth != null"
        }
      }
    }
  }
}
